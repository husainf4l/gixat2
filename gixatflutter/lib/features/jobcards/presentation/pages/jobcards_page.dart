import 'package:flutter/material.dart';

class JobCardsPage extends StatelessWidget {
  const JobCardsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(
          child: Text('Job Cards Page'),
        ),
      );
}
